<%= form_with(model: court, local: true) do |f| %>
  <%= render 'shared/error_messages', model: f.object %>
    <%= f.text_field :image, placeholder: "画像のURL" %>
    <%= f.text_area :text, placeholder: "名称（場所の名前）", rows: "1" %>
    <%= f.submit "SEND" %>
<% end %>